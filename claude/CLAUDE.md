# Global Preferences

## Git

- **Commits**: Each commit must be for a single feature or fix. Never combine unrelated changes in the same commit.
- **Pull requests**: PR titles should describe what the PR adds/changes, not "Merge X into Y". Use descriptive titles like "Add feature X and fix Y".
